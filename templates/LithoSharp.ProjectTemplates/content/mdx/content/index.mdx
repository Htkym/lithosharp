---
title: Welcome
---

# Start with static content

This document needs no React hydration. Add trusted components when you need interaction.

Restore the packaged worker once with `lithosharp restore-mdx bin/Debug/net10.0/worker` after `dotnet build`.
Normal builds never restore npm dependencies automatically.
